#!/bin/bash

sudo apt update && sudo apt upgrade -y

sudo apt install vim curl nmap netcat wireshark tor hashcat aircrack-ng wine shotcut gimp docker.io libreoffice gparted -y

sudo snap install discord

echo "Complete."
